// Fired by Preloader the instant its slide-open timeline completes — lets
// the cinematic landscape layer know when it's safe to fade itself in,
// without Preloader needing a direct reference to that layer.
export const PRELOADER_REVEALED = 'preloader:revealed'
